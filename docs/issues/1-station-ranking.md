# #1 여러 출발역으로 만날 곳 추천받기

## Goal

여러 사람이 출발역을 입력하면, 정적 수도권 전철망에서 모두에게 덜 먼 만남역을
추천한다.

## Scope

- 수도권 전철을 정점(역)·간선(같은 노선의 인접역)으로 표현한다.
- 인접역 간선에는 정적 기본 운행시간(분)을 두고, 환승은 초기 3분 패널티로
  시작한다.
- 후보 역마다 각 출발역까지 Dijkstra 정적 예상시간을 계산한다.
- `worst`(가장 오래 걸리는 사람의 예상시간)를 우선 최소화하고, 동률이면
  `total`(예상시간 합)이
  작은 순으로 후보 역 3개를 표시한다.
- 추천 이유와 사람별 정적 예상시간을 결과 카드에서 읽을 수 있게 한다.
- 잘못된 역 선택과 연결되지 않은 역 상태를 화면에서 처리한다.

## Execution checklist

- [x] 수도권 전철 전체의 노선·인접역·환승 원본을 라이선스와 범위까지 검증하고,
      불명확하면 `docs/loop/STATE.md`에 blocker로 남긴다.
- [x] 검증된 원본을 저장소의 정적 네트워크 데이터와 출처·범위 문서로 반영한다.
- [x] 정적 기본 운행시간과 환승 3분 패널티의 Dijkstra, `worst` → `total` 후보
      정렬을 테스트와 함께 구현한다.
- [x] 카카오 클라이언트와 런타임 외부 호출을 제거하고, 결과 화면에 정적 예상시간과
      추천 이유를 표시한다.
- [x] 전체 검사와 런타임 스모크 확인을 기록하고 `STATE.md`를 완료 상태로 갱신한다.

## Loop task board

### T1: 정적 네트워크 데이터

- 완료 조건: 제공받은 국가철도공단 XLSX에서 수도권 운행 노선의 인접역·정적 기본
  운행시간을 추출해 저장소 데이터로 포함한다. 출처, 라이선스, 기준일, 포함·제외
  노선을 문서화한다.
- 검증: 변환된 데이터에 강남역·홍대입구역이 있고, 모든 간선 시간이 양수인 테스트.
- 증거: `d3a56a9`, `scripts/generate-static-network.mjs`, `static-network.test.ts`

### T2: 환승과 N명 minimax 엔진

- 완료 조건: 같은 물리 환승역을 3분 간선으로 연결하고, 두 명 이상 출발역의
  `worst` → `total` 정렬 후보 3개를 계산한다.
- 검증: 환승 패널티와 동률 정렬을 포함한 단위 테스트.
- 증거: `d3a56a9`, `network.test.ts`, `static-recommendations.test.ts`

### T3: 정적 추천 API와 카카오 제거

- 완료 조건: 추천 Route Handler가 로컬 그래프만 사용하고, 카카오 모듈·키·외부
  fetch를 제거한다.
- 검증: Route Handler 테스트에서 외부 모듈 mock 없이 후보·입력 오류·연결 불가를
  검증한다.
- 증거: `d3a56a9`, `route.test.ts`, 런타임 외부 `fetch` 없음

### T4: N명 입력과 결과 화면

- 완료 조건: 사용자가 두 명 이상 출발역을 입력·추가·삭제할 수 있고, 후보 카드에
  사람별 정적 예상시간과 추천 이유가 표시된다.
- 검증: 입력 직렬화와 결과 표시 데이터의 단위 테스트.
- 증거: `d3a56a9`, `stations.test.ts`, N명 추가·삭제 UI

### T5: 종료 검증

- 완료 조건: 출처 문서, README, STATE, GitHub Issue 진행 기록이 코드와 일치한다.
- 검증: `pnpm test && pnpm lint && pnpm typecheck && pnpm build`, HTTP 스모크.
- 증거: `d3a56a9`, `pnpm test && pnpm lint && pnpm typecheck && pnpm build`, HTTP 스모크

> Source selected: 국가철도공단 `국가철도공단_도시광역철도_운행정보_20260630`
> (전체 도시광역철도 운행기관, 이용허락범위 제한 없음)을 정규화한다. 원본의
> 정거장 순서와 정적 도착·출발 시각으로 인접역 기본 운행시간을 만들고, 원본을
> 런타임에 조회하지 않는다. 같은 기관의 `도시광역철도_역사정보_20260630`으로
> 역명과 물리 환승역을 구분해 3분 환승 간선을 연결한다. 이 원본에는 GTX-A가
> 없으므로 이번 데이터 범위에서 제외하고, 별도 정적 원본이 확보될 때 추가한다.

## Non-goals

- 실시간 시간표·실제 대중교통 소요시간·외부 길찾기 API
- 카카오/네이버 지도 SDK와 API 키
- 맛집, 예약, 모임방 저장·공유 링크
- 버스·실시간 도착정보 최적화

## Done when

- 강남역과 홍대입구역처럼 출발역들을 선택하면 후보 역 3개와 사람별 정적 예상시간이
  화면에 보인다.
- 외부 API 호출과 API 키 없이 결과가 계산된다.
- `worst` 우선, 동률 시 `total` 우선의 minimax 정렬 규칙을 단위 테스트로 검증한다.
- 환승 3분 패널티가 경로 계산에 반영되는 것을 테스트로 검증한다.

## Checks

`npm test && npm run lint && npm run typecheck && npm run build`

## Issue

https://github.com/borobong2/gatigo-web/issues/1
